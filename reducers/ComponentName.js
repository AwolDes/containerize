
    const initialState = Object.assign({}, {
      SomeBool: false
    }
    export default functionComponentName"(state = initialState, action) {
      switch (action.type) {
        case 'SOME_STRING':
          const ChangeState = {
                SomeBool: true
            }
          });
          return Object.assign({}, state, ChangeState);
        default:
          return state;
      }
    }
    